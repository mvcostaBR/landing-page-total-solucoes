# Correção para GitHub Pages

Substitua na raiz do repositório:

- `index.html`
- `site.webmanifest`
- `scripts/validate.py` (somente para manter o validador sincronizado)

Adicione:

- `.nojekyll`

Não é necessário alterar os arquivos CSS, JavaScript, imagens ou ícones.

A estrutura da publicação deve manter `index.html` e `assets/` no mesmo nível.
